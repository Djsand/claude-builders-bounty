# PR Review: https://github.com/expressjs/express/pull/5871

_Generated by claude-review agent_

---

## Summary
This PR updates the `res.sendFile` implementation to correctly handle edge cases when the `root` option is provided alongside an absolute path, preventing a potential path traversal inconsistency. The change is minimal, focused, and accompanied by a new test case that validates the corrected behavior. Overall this is a high-quality, surgical fix.

## Risks
- The conditional logic change in `lib/response.js:234` alters existing behavior — any downstream code that relied on the previous (arguably incorrect) behavior would silently break without a deprecation warning.
- No changelog entry was added, which makes it harder for consumers to identify this behavioral fix when upgrading.

## Improvement Suggestions
- `lib/response.js:234`: Add an inline comment explaining *why* the `root` option takes precedence over an absolute path, since this is a non-obvious design decision.
- Consider adding a test case for the `root` option combined with a relative path to increase coverage of the new branch.
- The PR description could link to the related issue/bug report to provide context for reviewers.

## Confidence Score
High — The diff is small (< 30 lines), well-scoped, and the intent is clear from the test case added alongside the fix.